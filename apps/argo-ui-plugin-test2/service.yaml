apiVersion: v1
kind: Service
metadata:
  name: argo-ui-plugin-test2
spec:
  selector:
    app: argo-ui-plugin-test2
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP